export interface IViewer {
    // userId: string // ID of the user who is viewing
    streamId: string // ID of the stream being viewed
    viewerId: string
    // joinedAt: Date // Timestamp when the viewer joined
}
